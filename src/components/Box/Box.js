import styled from 'styled-components';
import { color, space, layout, flexbox, border, shadow } from 'styled-system';

const Box = styled('div')(space, color, layout, flexbox, border, shadow);

export default Box;
